# Project portability contract

`.guitarlab` is a versioned round-trip container. Save must preserve all referenced source media needed by the project; restore must validate before publication and create an independent project identity. A portable package that cannot be reopened is a failed persistence feature.
