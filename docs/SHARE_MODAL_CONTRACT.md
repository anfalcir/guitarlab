# Share modal contract

The Share modal is the single primary output surface in Studio. It separates editable-project persistence from final master delivery and must remain reachable directly from the top bar before Home. Output commands are not duplicated as primary actions under Options.
